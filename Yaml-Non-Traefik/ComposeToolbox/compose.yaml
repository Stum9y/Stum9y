services:
  composetoolbox:
    image: ghcr.io/bluegoosemedia/composetoolbox
    ports:
      - "4000:3000"
    environment:
      - NODE_ENV=${NODE_ENV}
    restart: unless-stopped
    volumes:
      - ./composetoolbox/data:/app/data
