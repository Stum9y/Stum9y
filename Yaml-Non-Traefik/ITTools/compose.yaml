services:
  it-tools:
    ports:
      - 8081:80
    container_name: it-tools
    stdin_open: true
    tty: true
    image: corentinth/it-tools:latest
    restart: unless-stopped

# networks:
#   default:
#     external: true
#     name: nginx
